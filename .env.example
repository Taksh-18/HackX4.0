# Backend database and deterministic demo mode
DISASTER_DB_PATH=./data/disaster.db
CDIS_OFFLINE_GEOCODE=1
CDIS_OFFLINE_EXTRACTION=1

# Optional LLM extraction provider
GROQ_API_KEY=
# OPENAI_API_KEY=
# EXTRACTION_BASE_URL=https://provider.example.com/v1
# EXTRACTION_MODEL=provider-model-name

# React uses /api through the Vite proxy by default. Set this only when the API
# is hosted at a separate public origin.
# VITE_API_BASE_URL=https://api.example.com
